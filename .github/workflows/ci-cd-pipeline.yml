name: CI/CD Pipeline

on:
  push:
    branches: [dev]
  workflow_dispatch:
    inputs:
      log_errors:
        description: 'Generate error logs on test failure (manual run)'
        required: false
        default: "false"

env:
  COINMARKETCAP_API_KEY: ${{ secrets.COINMARKETCAP_API_KEY }}
  COINMARKETCAP_SYMBOLS: "BTC"
  UPDATE_FREQ_SEC: 60

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    outputs:
      tests_result: ${{ steps.test_outcome.outputs.result }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          echo "Installing dependencies..."
          pip install uv
          uv sync --extra dev
      
      - name: Run tests
        id: run_tests
        continue-on-error: true
        run: |
          echo "Running tests..."
          uv run pytest src/tests/ -v

      - name: Set test outcome
        id: test_outcome
        run: |
          if [ "${{ steps.run_tests.outcome }}" = "success" ]; then
            echo "result=success" >> $GITHUB_OUTPUT
            echo "✅ Tests completed successfully!"
          else
            echo "result=failure" >> $GITHUB_OUTPUT
            echo "❌ Tests failed"
          fi

  error_logs:
    name: Generate Error Logs
    needs: test
    if: needs.test.outputs.tests_result == 'failure' && github.event.inputs.log_errors == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          echo "Installing dependencies..."
          pip install uv
          uv sync --extra dev
      
      - name: Generate error logs
        run: |
          echo "=== Test Failure Report ===" > error.log
          echo "Timestamp: $(date)" >> error.log
          echo "Branch: ${{ github.ref }}" >> error.log
          echo "Commit: ${{ github.sha }}" >> error.log
          echo "" >> error.log
          uv run pytest src/tests/ --tb=long >> error.log 2>&1

      - name: Upload error logs
        uses: actions/upload-artifact@v4
        with:
          name: test-error-logs-${{ github.run_number }}
          path: error.log

  build:
    name: Build and Push Docker Image
    needs: test
    if: needs.test.outputs.tests_result == 'success'
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/crypto-dashboard:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/crypto-dashboard:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
  deploy:
    name: Deploy to Azure
    needs: build
    if: success()
    runs-on: ubuntu-latest
    steps:
      - name: Azure CLI Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      
      - name: Force Azure to Pull Latest Image
        run: |
          # Update to use the specific SHA tag, then switch back to latest
          az webapp config container set \
            --name ${{ secrets.AZURE_WEBAPP_NAME }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --docker-custom-image-name ${{ secrets.DOCKERHUB_USERNAME }}/crypto-dashboard:${{ github.sha }}
          
          # Restart to ensure the new image is pulled and running
          az webapp restart \
            --name ${{ secrets.AZURE_WEBAPP_NAME }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }}

          echo "✅ Deployed SHA: ${{ github.sha }}"